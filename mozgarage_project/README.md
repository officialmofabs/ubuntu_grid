# MozGarage - Deployable Git Project (Tarball)

This archive contains the MozGarage Ultimate Deployment Standard project scaffold.
Run `sudo bash mozgarage_deploy_standard.sh` to scaffold and bootstrap the stack.

Edit the top of the script to replace placeholders (CF_API_TOKEN, SSH_USER, SSH_HOST, etc).
